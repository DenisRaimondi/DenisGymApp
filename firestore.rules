rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /gymapp/{userId} {
      // Solo denis.raimondi@gmail.com può accedere
      allow read, write: if request.auth != null
        && request.auth.token.email == 'denis.raimondi@gmail.com'
        && request.auth.uid == userId;
    }
  }
}
